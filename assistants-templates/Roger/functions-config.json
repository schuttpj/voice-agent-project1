{
  "functions": [
    {
      "name": "getAssistantResponse",
      "description": "Provides contextual information based on a given query.",
      "parameters": {
        "type": "object",
        "properties": {
          "inspiration": {
            "type": "string",
            "description": "The query or topic to get information about."
          },
          "assistantName": {
            "type": "string",
            "description": "The name of the assistant to use for the response.",
            "default": "Roger"
          }
        },
        "required": ["inspiration"]
      }
    }
  ]
} 